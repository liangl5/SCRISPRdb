var mysql = require('mysql');

var con = mysql.createConnection({
	host: "localhost",
	user: "mubil",
	password: "***Mubil520^^^",
	database: "SCRISPRdb"

});

con.connect(function(err){
	if (err) throw err;
	con.query("SELECT DISTINCT domain FROM Complete_Genome", function(err, result, fields) {
		if (err) throw err;
		for (i = 0; i < result.length; i++) {
			console.log(result[i].domain);
	
	});
});
